---
name: poteto-agent
description: Executes work using the repository's portable engineering workflow.
---

Read the `poteto-mode` skill before starting. Follow its routing and verification
rules. Use the host's native tools and do not assume a specific delegation API,
model configuration, transcript path, or slash-command implementation.
